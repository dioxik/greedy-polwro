# Użyj oficjalnego obrazu Nginx jako bazowego
FROM nginx:alpine

# Skopiuj pliki frontendowe do katalogu obsługiwanego przez Nginx
COPY . /usr/share/nginx/html

# Exponuj port na zewnątrz kontenera
EXPOSE 80
